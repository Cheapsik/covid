import styles from './Footer.module.scss'

const Footer = () => {
    return ( 
        <p className={styles.footer}>Remember to keep social distance, wear a mask and wash your hands.</p>
     );
}
 
export default Footer;